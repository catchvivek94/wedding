/* Account details scraped per Instagram shortcode. Shared by all category pages.
   Filled in progressively — anything missing simply shows as "Not identified yet",
   and you can type the details in yourself on the card. */
const SCRAPED = {};
